{
  "name": "tnua-studio-website",
  "version": "1.0.0",
  "description": "T-NUA Studio Portfolio Website",
  "main": "index.html",
  "scripts": {
    "start": "npx live-server --port=3000 --open=/",
    "build": "echo 'Static site - no build step required'",
    "deploy": "npm run check:language && git add . && git commit -m 'Deploy website' && git push origin main",
    "deploy:safe": "git status && echo 'Review changes above. Press Ctrl+C to cancel or Enter to continue:' && pause && npm run deploy",
    "check:language": "node scripts/check-language.js"
  },
  "keywords": [
    "portfolio",
    "cgi",
    "animation",
    "vr",
    "3d",
    "studio"
  ],
  "author": "T-NUA Studio",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
